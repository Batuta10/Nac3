<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.fiap.exercicios.listview.RM78792">

    <application
        android:allowBackup="true"
        android:icon="@drawable/pizzalogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pizzalogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity" android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".PizzaActivity" />
        <activity android:name=".PreferencesActivity"/>
    </application>

</manifest>